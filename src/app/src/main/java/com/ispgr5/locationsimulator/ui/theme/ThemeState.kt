package com.ispgr5.locationsimulator.ui.theme

/**
 * The Theme State to controll whether the Dark Mode is On or not
 */
data class ThemeState (
    val isDarkTheme: Boolean = false //whether the dark mode is on or not (Light Mode is standard)
)